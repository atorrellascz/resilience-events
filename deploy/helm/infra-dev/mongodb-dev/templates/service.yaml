apiVersion: v1
kind: Service
metadata:
  name: mongodb
  labels:
    app: mongodb
spec:
  clusterIP: None           # headless, like SQL Server
  selector:
    app: mongodb
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}