apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catalog-api
  labels:
    app: catalog-api
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: catalog-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s