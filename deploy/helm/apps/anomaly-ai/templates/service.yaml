apiVersion: v1
kind: Service
metadata:
  name: anomaly-ai
  labels:
    app: anomaly-ai
spec:
  type: ClusterIP
  selector:
    app: anomaly-ai
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP