replicaCount: 1

image:
  repository: rep/anomaly-ai
  tag: "0.1.0"
  pullPolicy: IfNotPresent

service:
  port: 8080

resources:
  requests:
    cpu: 100m
    memory: 256Mi          # Python + SQLAlchemy, in between Go and .NET
  limits:
    cpu: 500m
    memory: 512Mi