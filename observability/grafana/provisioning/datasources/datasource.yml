# Connects Grafana to Prometheus automatically on startup (config as code)
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy                    # Grafana queries Prometheus over the internal network
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 15s              # matches Prometheus's scrape_interval
